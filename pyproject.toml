[project]
name = "template-repository"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sentry-sdk>=2.30.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]

[tool.ruff]
line-length = 120
